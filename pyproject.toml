[project]
name = "cross-harness"
version = "0.1.0"
description = "Claude Code orchestrator to Codex execution harness"
requires-python = ">=3.11"

[project.scripts]
cross-harness = "cross_harness.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

